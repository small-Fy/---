<!-- <van-cell-group>
  <van-field
    value="{{ value }}"
    placeholder="请输入用户名"
    border="{{ false }}"
    bind:confirm="onChange"
  />
</van-cell-group>

<van-popup show="{{ showFlag }}" bind:close="onClose" custom-style="height: 95%;" position="bottom" overlay="{{false}}">
  <view class="cityList">
    <view class="cityItem" wx:for="{{cityList}}" wx:key="{{index}}" bindtap="chooseCity" data-item="{{item}}">
      {{item.title}}
    </view>
  </view>
</van-popup>

<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="30" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location style="width: 100%; height: 1340rpx;"></map> -->